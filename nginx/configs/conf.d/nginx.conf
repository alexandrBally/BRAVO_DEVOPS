upstream backend {
   server backend:4000;
 }

 server {
   listen 80;

   location / {
       proxy_pass http://frontend:3000;
   }


# 21   location /api/ {
# 22       rewrite /api/(.*) /$1 break;
# 23       proxy_pass http://backend/;
# 24   }
}
